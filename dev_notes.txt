85% accuracy for final_model.keras (on my handwriting) acheived.


Frontend: use flutter and dart. flutter's canvas widget "CustomPainter" for drawing.
(DONE) Backend: Python FastAPI (deployed as Vercel serverless functions or a separate Railway/Fly.io service)
(DONE) ML: TensorFlow + Keras CNN, exported to ONNX, loaded at startup
DB: Supabase (Postgres) for high scores; free tier is generous and works great with Next.js


# press ctrl+c in terminal to stop the server when done
# for full app, will need to run both backend and frontend servers:
# 1. in backend folder: uvicorn main:app --reload
# 2. in frontend folder: npm run dev

cd sudo_tay
flutter run -d windows
FILES: 
collect_data.py  ->  handwriting/ folder (raw data)
model.py         ->  mnist_model.keras (base knowledge)
train_on_data.py ->  final_model.keras (personalized model)
main.py          ->  serves final_model.keras to the world
frontend         ->  talks to main.py via HTTP


need a component that has sovable sudoku puzzles
    eventually expand to extreme 1-16 puzzles?
"notes" component-- making i can make it so that the user can write in "red" whatever they want and the model will ignore those
i think that the model needs to only evaluate the final "solved" puzzle for mistakes, and compare that with a reference to determine accuracy
i can mae the "notes" disappear before passing the player's puzzle on to the model?

use a backtracking algorithm to generate a fully solved board, then remove cells while ensuring a unique solution remains solvable. 
Libraries like py-sudoku or sudoku-solver on PyPI handle this well.